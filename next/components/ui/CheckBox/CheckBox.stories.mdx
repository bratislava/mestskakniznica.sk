import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { CheckBox } from './CheckBox';

<Meta title="components/atoms/CheckBox" decorators={[withDesign]} />

# CheckBox

Styled Button, that is used as a checkbox throughout all CityLibrary web app.

## Props

CheckBox accepts all default HTMLInputProps.

<Canvas>
  <Story
    name="CheckBox"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    argTypes={{
      id: {
        name: 'id',
        type: { name: 'string', required: true },
        defaultValue: 'id1',
      },
    }}
    args={{
      className: '',
      checked: true,
      children: 'Checkbox label',
      disabled: false,
    }}
  >
    {CheckBox.bind({})}
  </Story>
</Canvas>

<ArgsTable of={CheckBox} />

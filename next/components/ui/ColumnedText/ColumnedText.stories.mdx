import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import ColumnedText from './ColumnedText'

<Meta title="components/ColumnedText" decorators={[withDesign]} />

# ColumnedText

This is our ColumnedText

## Single ColumnedText

TODO: Write Docs

<Canvas>
  <Story
    name="Single ColumnedText"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{}}
  >
    {ColumnedText.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ColumnedText} />

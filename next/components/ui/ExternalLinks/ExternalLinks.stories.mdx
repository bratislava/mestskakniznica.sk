import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import ExternalLinks from './ExternalLinks';

<Meta title="components/ExternalLinks" decorators={[withDesign]} />

# ExternalLinks

This is our ExternalLinks

## Single ExternalLinks

TODO: Write Docs

<Canvas>
  <Story
    name="Single ExternalLinks"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{}}
  >
    {ExternalLinks.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ExternalLinks} />

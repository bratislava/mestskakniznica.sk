import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { Radio } from './Radio';

<Meta title="components/atoms/Radio" decorators={[withDesign]} />

# Radio

Styled Button, that is used as a radio throughout all CityLibrary web app.

## Props

Radio accepts all default HTMLInputProps.

<Canvas>
  <Story
    name="Radio"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: '',
      checked: false,
      children: 'Radio button label',
      disabled: false,
    }}
  >
    {Radio.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Radio} />

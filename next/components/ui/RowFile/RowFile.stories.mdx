import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { RowFile } from './RowFile';

<Meta title="components/atoms/RowFile" decorators={[withDesign]} />

# RowFile

This is our RowFile

## Single RowFile

TODO: Write Docs

<Canvas>
  <Story
    name="RowFile"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: '',
      type: 'Zmluva',
      title: 'Nazov dokumentu',
      metadata: 'Metadata',
      dateAdded: 'Pridane 12. novembra 2020',
      icon: 'PDF',
    }}
  >
    {RowFile.bind({})}
  </Story>
</Canvas>

<ArgsTable of={RowFile} />

import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { SearchBar } from './SearchBar'

<Meta title="components/molecules/SearchBar" decorators={[withDesign]} />

# SearchBar

Styled Input, that is used as a searchbar throughout CityLibrary web app.

## Props

Input accepts all InputProps from component Input in addition to custom Props that are as follows:

<Canvas>
  <Story
    name="SearchBar"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: '',
      inputClassName: '',
      hasError: false,
      disabled: false,
      labelContent: 'Label',
      helpText: 'Help text',
      placeholder: 'Placeholder...',
      value: '',
    }}
  >
    {SearchBar.bind({})}
  </Story>
</Canvas>

<ArgsTable of={SearchBar} />
